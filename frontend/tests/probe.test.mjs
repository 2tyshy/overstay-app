// Intentionally empty — superseded by dates.test.mts and visaRules.test.mts.
// Can't rm this file from the sandbox, so it lives as a stub.
import { test } from 'node:test'
test('probe placeholder', () => {})
